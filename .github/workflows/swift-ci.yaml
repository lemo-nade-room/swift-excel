name: Swift CI

on:
  pull_request:
    paths:
      - '.github/workflows/swift-ci.yaml'
      - '.swiftlint.yml'
      - '**/*.swift'

jobs:
  SwiftLint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: GitHub Action for SwiftLint
        uses: norio-nomura/action-swiftlint@3.2.1

      - name: GitHub Action for SwiftLint with --strict
        uses: norio-nomura/action-swiftlint@3.2.1
        with:
          args: --strict

      - name: GitHub Action for SwiftLint (Only files changed in the PR)
        uses: norio-nomura/action-swiftlint@3.2.1
        env:
          DIFF_BASE: ${{ github.base_ref }}

      - name: GitHub Action for SwiftLint (Different working directory)
        uses: norio-nomura/action-swiftlint@3.2.1
        env:
          WORKING_DIRECTORY: Source

  SwiftUnitTest:
    runs-on: ubuntu-latest
    needs:
      - SwiftLint
    steps:
      - uses: actions/checkout@v4

      - uses: actions/cache@v4
        with:
          key: test-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('Package.resolved') }}
          path: ${{ github.workspace }}/.build

      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "5.10"

      - name: Remove SwiftLint plugin from Package.swift
        run: |
          sed -i '/.plugin(name: "SwiftLintBuildToolPlugin", package: "SwiftLint"),/d' Package.swift

      - run: swift build

      - run: swift test